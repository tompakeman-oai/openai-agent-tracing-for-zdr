{
    "scripts": {
      "dev": "source .venv/bin/activate && uvicorn api:app --host 0.0.0.0 --port 8000 --timeout-keep-alive 600",
      "install": "uv venv && source .venv/bin/activate && uv sync --active"
    }
  }